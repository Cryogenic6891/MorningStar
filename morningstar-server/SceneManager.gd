extends Node

# Scene Schema
# SceneName : {
# Layout : {} zone, Mesh, Navigation, body/collision
# Events : {} spawn locations,
# Entities : {} - inherent to scene, position, rotation, model (size, mesh, body/collision), behaviour
# Static : {} - static entities
